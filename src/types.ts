/**
 * Shared types for @almadar/patterns
 *
 * @packageDocumentation
 */

/** Schema describing a single prop in a pattern's propsSchema */
export type PatternPropDef = { types?: string[]; required?: boolean };
